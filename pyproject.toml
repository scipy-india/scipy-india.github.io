[project]
name = "scipy-india-website"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sphinx>=7.4",
    "pydata-sphinx-theme>=0.18",
    "myst-parser[linkify]>=3.0",
    "ablog>=0.11",
    "sphinx-design>=0.6",
    "sphinx-copybutton>=0.5",
    "sphinx-togglebutton>=0.3",
    # bump with caution. see _prefer_html_meta_description in 2026/conf.py
    "sphinxext-opengraph==0.13.0",
    # to render the per-page social cards that sphinxext-opengraph generates
    "matplotlib>=3.8",
    "sphinx-autobuild",
    "pyyaml>=6",
]

[dependency-groups]
dev = []

[tool.uv]
# This is a website, not a distributable package, so there is no wheel to build.
# Tell uv to install only the dependencies, never the project itself.
package = false
